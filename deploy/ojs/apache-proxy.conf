# The OJS container sits behind the nginx reverse proxy, which terminates TLS.
# Mark proxied HTTPS requests as HTTPS so OJS builds https:// URLs and
# force_ssl does not cause redirect loops.
SetEnvIf X-Forwarded-Proto "^https$" HTTPS=on
